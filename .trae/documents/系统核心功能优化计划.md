好的，我们将开始对系统进行优化。根据之前的分析，我们将分步骤解决核心的 Mock 数据和逻辑不完善问题。

**优化计划：**

**第一阶段：强化组织者权限验证 (Organizer Auth)**
1.  **目标**: 确保只有登录且具备适当角色的用户才能访问 `(organizer)` 路由下的页面。
2.  **行动**:
    *   修改 `src/app/[locale]/(organizer)/layout.tsx`。
    *   引入 `getServerSession` 获取当前会话。
    *   如果没有会话或用户未登录，重定向到登录页。
    *   (可选) 如果系统有明确的 "Organizer" 角色字段，也应一并检查。

**第二阶段：移除前端 Mock Fallback 逻辑**
1.  **目标**: 让前端真实反映后端的错误状态，不再“假装成功”。
2.  **行动**:
    *   修改 `src/app/[locale]/(organizer)/my-events/[eventId]/page.tsx`。
    *   删除 `calculateMatches` 和 `handleSendEmails` 函数中 `catch` 块里的 Mock 逻辑。
    *   确保错误信息能通过 `toast.error` 正确展示给用户。

**第三阶段：实现真实的邮件发送服务 (Email Service)**
1.  **目标**: 替换后端的 Mock 实现，集成真实的邮件发送逻辑。
2.  **行动**:
    *   选择 **Resend** 作为邮件服务提供商（因为它现代、简单且通常有免费额度，且Next.js生态支持好）。
    *   安装 `resend` SDK: `npm install resend`。
    *   修改 `src/app/api/events/[eventId]/emails/route.ts`：
        *   引入 Resend 客户端。
        *   实现 `sendParticipantList`：生成 HTML 表格，包含所有参与者信息。
        *   实现 `sendMatchResults`：遍历匹配结果，为每一对匹配的用户分别发送包含对方联系方式的邮件。
    *   **注意**: 这一步需要您提供 Resend API Key。如果暂时没有，我会先搭建好代码结构，您只需填入 Key 即可生效。

**执行顺序：**
我们将按照 **权限验证 -> 移除 Mock -> 邮件服务** 的顺序执行。

准备好后，我将从权限验证开始。
